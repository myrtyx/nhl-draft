# Журнал решений

Что менялось в движке, почему и чем проверено. Пишется, чтобы советы не
скакали между пиками, а расхождения можно было предъявить.

## Контроли, которые гоняются перед каждым выкатом

| проверка | как должно быть |
|---|---|
| калибровка | средняя команда берёт ≈6.000 категорий из 12 |
| составы в симуляции | все 12 легальны, старт 12/12, лавка ≤4 |
| скорость | prepare под 1 с, доска под 100 мс |
| сборка Pages | SHA совпадает с HEAD |

Прогон: `node tools/check.js`

## 22.09.2026 — метрика: шанс взять 7 из 12 вместо суммы категорий

**Повод.** Мартин: «ебать в сраку этих центров, почему мы опять видим только
их». Слоты C у него были полны, а доска показывала 15 центров подряд.

**Что оказалось.** Два бага и одна методическая ошибка.

1. *Состав раздувался до 17.* Если слот полон своими, добор туда никого не
   ставил, и кандидат вписывался СВЕРХ состава — бесплатно, никого не
   вытесняя. Центры от этого раздувались. Починено: вытесняется слабейший
   с лавки, состав всегда 16, старт 12/12.
2. *Лавка была бездонной.* `BENCH` не был объявлен вообще, `slotFor` возвращал
   `'BN'` без предела. Модель могла набрать семь защитников.
3. *Считалась не та величина.* Складывались ожидаемые категории, а неделя
   выигрывается по большинству — 7 из 12. Категория на 68% уже почти взята,
   и лить в неё ещё — трата пика; четыре вратарские висели на 46-48% и
   берутся все разом. Заменено на свёртку P(≥7).

**Что это изменило.** По сумме первым шёл Вигар (хиты в уже выигранную
категорию). По свёртке первым идёт вратарь: +12.4 п.п. к шансу, Вигар вне
первой дюжины. Интуиция Мартина («Вигар — хрень») подтвердилась счётом.

**Улика.** Добсон даёт тот же blk +12, что и Вигар, но стоил −5.5 против
+5.5. Вся разница — хиты, то есть премия за категорию, которая уже выиграна.

## 22.09.2026 — игрок считается по лучшему слоту

**Повод.** Гаутье с рангом Yahoo 29 стоял на 218-м месте.

**Что оказалось.** `slotFor` брал первую позицию из `p.pos`, а первым всегда
идёт C — самая забитая группа, где добор и так закроет слот сильным игроком.
Любой C/LW/RW выглядел пустышкой: Гаутье на C давал −7.5, он же на LW +20.3,
Джеки на RW +43.8. Починено: перебираются свободные слоты, считается лучший.

## 22.09.2026 — раскладка через аугментирующий путь

**Повод.** Зибанейд (C/RW) сел на C, второй слот центра оказался занят, и
Кросби показывался скамейкой — хотя в Yahoo Зибанейд просто сдвигается на RW.

**Починено.** Новый игрок вытесняет соседа, если тому есть куда уйти.
Кросби 5.96 → 9.68, стартеров стало 12 из 12.

**Грабли.** Первая версия зациклилась: `for (const [q] of at)` с `at.set`
внутри — Map дописывает в конец, итератор видит снова. Итерация по копии.

## 22.09.2026 — почему советы скакали

Три раза подряд назывались разные игроки на один пик. Причина не в движке:
тестовые прогоны помечали всех взятых единицей, а движок ищет своих по метке
`'ME'` — и считал от ПУСТОГО состава. Сайт при этом считал правильно.

**Инструмент.** `scratchpad/board.js` читает SEED прямо из `index.html` и
расставляет метки той же функцией, что и сайт. Любой прогон — только через него.

## Открытые вопросы

- Минимум вратарских выходов (3/неделю) — удваивается ли он в двухнедельном
  матче 1–14 февраля? Спросить у владельца лиги.
- Репозиторий публичный, ник `myrtyx` совпадает с ником в Fantrax — соперники
  могут найти доску.

## 22.09 — почему чат расходился с сайтом

Мартин: «почему ты говоришь одно, а на сайте другое». Расхождение было
настоящее, но врал **чат, а не сайт**.

Причина в `futureFill`: число сделанных пиков он берёт из `Object.keys(taken).length`.
В своих проверочных скриптах я дописывал игрока как `taken[имя]='ME'` — пик при
этом не тратился, и добор продолжал достраивать состав на все 12 оставшихся
пиков. Получался состав из **17 игроков вместо 16**: кандидат приходил сверх
команды, бесплатно. Отсюда «+20 п.п.» в чате против «+9» на сайте.

Второй заход — падинг очереди пустышками `__пN` — тоже врал: пустышки не
убирают игроков из пула, добор оставался завышенным, база вырастала с 41% до 50%.

Правильная модель (`scratchpad/at2.js`): пики 44–58 разыгрываются реальными
игроками по рангу Yahoo, кандидат встаёт на свой пик, состав остаётся 16.
Проверка: во всех парных расчётах `roster.length === 16`.

Движок считает верно. `catDelta` вытесняет ровно одного — филлера с того же
слота, а если слот полон, худшего со скамейки, — и состав держится на 16.

**Договорённость по числу.** На сайте две колонки, и я называл не ту, по которой
сортировка. Дальше в чате я называю **ЦЕНУ** (vorp) — первую колонку, по ней же
и сортировка. «vs РАУНД» (score) — сырой прирост шанса, без поправки на то, что
похожего игрока можно взять и через круг.

Побочное: прогноз по рангу до #59 съедает Gauthier, Barkov, Hellebuyck, Crosby.

## 22.09 — доска слепа к крайним: инструмент `tools/pick.js`

**Повод.** Пик #59. Доска показала сверху Кросби, Веймелку, Хеллебака и восемь
защитников — а Кемпе с рангом Yahoo 47 оказался на **273-м месте, цена −10.8**.

**Причина та же, что и раньше** — `futureFill` строит план добора по рангу
Yahoo. Мои пустые слоты LW 2 и RW 2 план закрывает Конечны, Кейном и
Марчментом, и любой настоящий крайний вытесняет из плана равного себе:
прирост около нуля. У защитников и вратарей такого нет — там план ставит
слабых, и кандидат выглядит дорогим. То есть колонка ЦЕНА сейчас системно
занижает ту самую группу, из которой мне брать четыре слота из шести.

**Инструмент.** `tools/pick.js` — три замера, не зависящие от плана добора:

| режим | что делает |
|---|---|
| `solo Имя ...` | состав заморожен, меняется ровно один игрок — честное сравнение |
| `live Имя ...` | дожитие до моих ближайших трёх пиков, шум ранга σ=9 |
| без аргументов | и то, и другое по верхушке доски |

σ=9 — не выдумка, а измеренная средняя ошибка ранга Yahoo против реальных
пиков этой лиги (43 пика).

**Грабли.** Первая версия `solo` вырезала из базы последнего игрока подряд, и
вратарь-кандидат вставал **третьим** сверх слотов G:2 — минимум выходов
прыгал до 98%, Хеллебак показывал 64.1%. Вырезать надо филлера того же типа.

**Что замеры дали на #59.**

- Чистый вклад: Кросби 64.2%, Раддиш 56.5, Фокс 56.8, Веймелка 55.2,
  Хеллебак 55.2, Кемпе 53.7. Кемпе недооценён доской, но Кросби всё равно
  лучше — дефект реальный, решения не меняет.
- Хеллебак **не лучше Веймелки**: в проекциях Yahoo у него 60 игр, 27 побед,
  .902 и ранг 53. Довод «обрыв у вратарей, Хеллебак наверху» не подтвердился.
- Второй вратарь обязателен в паре #59/#62: без него минимум трёх выходов
  берётся в 31% недель, и все четыре вратарские категории уходят (p7 18–21%
  против 49%). Ждать до #83 нельзя — Веймелка доживает туда в 16%, полный
  прогон «вратарь на #83» даёт 58.0% против 62.5%.

## 22.09 — прогноз команд вместо мест в таблице (ОТМЕНЕНО в тот же день, см. ниже)

**Повод.** Мартин: «проставь projected стат каждой команды красиво, чтобы мне
легко было выбирать». И заявка на стратегию: уходить в мультипозиционных.

**Что было.** Блок «Соперники» показывал только место команды 1–12 в каждой
категории и считал его по тому, что уже взято. Команды с разным числом пиков
сравнивались напрямую: у кого пиком больше, тот и «лидер».

**Что стало.** `projectAll` в движке достраивает **каждую** команду до 16
игроков общим добором (игрок уходит одной команде и пропадает из пула) и
считает сезонные суммы по всем двенадцати категориям. Таблица — тепловая
карта по месту, плюс два ряда внизу: порог шестого места и **чего мне до него
не хватает**. Это и есть список того, за чем идти: на #60 у меня BLK −184,
FW −165, A −22 при запасе HIT +165 и SV +109.

**Под мультипозиции.** В списке игроков позиции подсвечиваются те, что
закрывают мои пустые слоты, и ставится `×2`, если игрок затыкает две дыры
сразу: `C/LW/RW ×2` у Миллера. В колонке «гибк.» — сколько таких уже взято
(у меня 1, у AleksandrsZ и unwill по 3).

**Грабли.** `posCell` и `fits` объявил ниже места использования — страница
падала с `Cannot access before initialization` ещё до отрисовки таблицы.
Поймано скриншотом в браузере, а не чтением кода.

## 22.09 — три вкладки вместо одного экрана

**Повод.** Мартин: «в том маленьком окошке мне не видно, сделай отдельно
кнопку в меню… и ещё отдельным меню кто что пикнул, чтобы было понятно, кто
что пикнет скоро».

**Что стало.** Шапка получила три вкладки.

| вкладка | что внутри |
|---|---|
| Доска | как было: «брать сейчас», список игроков, мои категории, ростер |
| Команды | прогноз всех двенадцати на конец драфта во всю ширину + составы |
| Пики | ближайшие 14 ходов по именам и лента всей доски змейкой |

«Прогноз команд» переехал из боковой колонки 340 px во всю ширину — двенадцать
категорий на двенадцать команд в узкий столбец не влезали, это и была жалоба.

**Лента.** Раунды сверху вниз, колонки — команды. Прошедшие пики фактом,
ближайшие — пунктиром с жёлтым, чтобы прогноз нельзя было спутать с фактом.
Раскладка по колонкам считается тем же `teamOf`, что и движок: змейка в
Fantrax сбивается на третьем раунде, и второй реализации этого правила в
проекте быть не должно.

**Цена.** `projectAll` и `nextPicks` считаются только для открытой вкладки —
иначе доска платила бы достройкой двенадцати составов на каждый клик.

**Грабли.** `hidden` не скрывал вкладки: у `.layout` и карточек свой
`display`, а он перебивает `display:none` из стилей браузера. Лечится
`[hidden]{display:none!important}`. Поймано скриншотом — консоль молчала.

## 22.09 — довод про два пика проверяется парой: `pick.js pair`

**Повод.** Мартин на #62: «беру Батерсона, добрать чела с fw легко будет».
Это довод про ДВА пика, а `solo` считает один — предъявлять ему 43.8% против
54.0% было бы подменой предмета спора.

**Проверено.** Про фейсоффы он прав: центры с 750–840 FW стоят на рангах
129–369 (О'Райлли 767 @129, Стром 775 @132, Стаал 838 @369) и до #83 доживают
в 100% прогонов. Дыры FW −165 это закрывает.

**Чего это стоит.** Пара на #62+#83: Кросби+Миллер 59.3%, Батерсон+Миллер
53.6%, Батерсон+дешёвый центр 48.6%. Разница не в фейсоффах, а в передачах:
Кросби 49 A против 40, и хиты Батерсона (127) льются в категорию с запасом
+165. Дожидаться Батерсона на #83 тоже нельзя — он доживает в 12%.

**Инструмент.** `node tools/pick.js pair 'А+Б' 'В+Г'` — вырезает двух филлеров
своего типа под каждого кандидата, состав проверяется на 16.

## 22.09 — таблица команд показывает факт, а не модель

**Повод.** Мартин: «что за прогноз у тебя на людях, я хочу projected stats на
данный момент. Как ты всучил Tomashek 2500 FW, когда у него один центр. Они не
боты выбирать, как ты думаешь».

**Он прав, и ошибка моя.** `projectAll` достраивал каждую команду до 16 человек
планом по рангу Yahoo. План лепил в пустые слоты центров — и у Tomashek,
где из центров один Ахо, выходило 2545 вбрасываний. Стояло это в таблице
рядом с настоящими числами, без всякой пометки, что две трети столбца —
выдумка модели. Столбец статистики читается как факт; если он моделью — он
врёт, каким бы правильным ни было намерение уравнять составы.

Отдельно неверно было допущение: план предполагал, что лига пикает по списку
Yahoo. Измеренная ошибка этого предположения — 8.8 пика на ход.

**Что стало.** `standings` — сумма сезонных проекций Yahoo по РЕАЛЬНО взятым
игрокам. Никакого добора, никаких весов состава: каждое число в таблице
пересчитывается руками по пяти карточкам. Где игроков типа нет — прочерк, и
место считается только среди тех, у кого они есть (вратаря взяли 8 из 12).
Добавлена колонка «пик»: составы неравной длины теперь видно, а не
замазано моделью.

**Насколько это меняло картину.** Строка «мне не хватает» по модели:
FW −165, BLK −184, HIT +165. По факту: FW **+524**, BLK **+107**, HIT +163, а
дыры — G −25, SOG −14, A −6. То есть модель показывала дефицит ровно там, где
у меня запас, и на этом строился совет «добери фейсоффы».

**Удалено.** `projectAll` и `rosters` вырезаны из движка — после замены обе
мёртвые.

## 22.09 — прогноз чужих пиков: 17% попаданий, σ был вдвое занижен

**Повод.** Мартин: «что за фигня в змейке на нашем сайте? Как ты проджетишь то,
что невозможно, я почти уверен, что почти все твои пики не сойдутся».

**Проверил обратным прогоном по 59 сыгранным пикам** (`scratchpad/back.js`):
прогноз строится только по первым n−1 пикам и сверяется с тем, что было.

| что предсказываем | сбывается |
|---|---|
| имя следующего пика | **17%** (8 из 47) |
| состав ближайших 5 пиков | 39% |
| состав ближайших 12 пиков | 57% |
| состав ближайших 24 пиков | 67% |

Он прав: пять названных имён из шести — мимо. Список «Кто пикнет дальше»
убран, в ленте будущие чужие ячейки теперь пустые.

**Что вместо.** Единственное, что от прогноза нужно, — доживёт ли игрок до
моего хода. Это множество, а не имя, и оно предсказуемо.

**Калибровка (`scratchpad/calib2.js`).** σ=9 был взят из «средней ошибки ранга
8.8 пика» — и оказался самоуверенным вдвое:

| σ | обещано в корзине 0–10% | реально дожили | средняя ошибка |
|---|---|---|---|
| 9 | 5% | **26%** | 11.0 п.п. |
| 14 | — | — | 5.3 п.п. |
| 18 | — | — | 4.7 п.п. |
| **24** | — | — | **3.7 п.п.** |
| 30 | — | — | 5.1 п.п. |

1400 наблюдений. При σ=24 модель обещает 72% дожития, сбывается 73%.

**Что это поменяло в советах.** Кросби до #83 доживал «в 2%» — на деле 24%.
Батерсон «12%» — на деле 37%. Оба числа я называл Мартину.

**Грабли, найденные по пути.**

1. Первая версия проверки брала состав команды из НАСТОЯЩЕГО драфта внутри
   симуляции — утечка правды в модель. Переписано на состав того же прогона.
2. Шум считался дважды: в движке суммой трёх равномерных, в `pick.js` —
   нормальным. Расхождение 6 п.п. по Кросби. Теперь одна реализация —
   `core.survive`, `pick.js` только печатает.
3. Множитель суммы равномерных: у (r+r+r−1.5) отклонение 0.5, нужен 2σ.
   С 1.155 шум выходил 13.9 при заявленных 24.

**Удалено.** `nextPicks` — после замены мёртвая.

## 22.09 — блоки не дефицит: их дают даром в конце драфта

**Повод.** Мартин по таблице фактов: «в данный момент не наблюдаю дыры».

**Он прав наполовину.** Общий BLK 350 у меня выше порога на 107, но это
потому, что защитников у меня два, а у девяти команд — один. На одного
взятого защитника я третий: TOMIKS 146, klavs 137, **я 128**.

**Но дыры и правда нет, и вот почему.** Блоки — самая дешёвая категория в
драфте:

| игрок | ранг Yahoo | BLK |
|---|---|---|
| Weegar | 159 | 184 |
| Dobson | 145 | 182 |
| Lindell | **503** | 180 |
| McNabb | **590** | 180 |
| McCabe | **569** | 180 |

Те же 180 блоков лежат на 430 рангов глубже. Чистый вклад: Вигар 49.0%,
Макнэбб 46.8% — 2.2 п.п. за 430 пиков разницы. Тратить на блоки ход раньше
двенадцатого раунда незачем, и мой довод «BLK 32% — главная дыра» этого не
учитывал: дыра есть, но закрывается она бесплатно.

## порядок двух ходов решается редко: `pick.js order`

Вопрос «сейчас центра или вратаря» выглядит как вопрос о том, кто сегодня
лучше. На самом деле это вопрос о том, кто останется к следующему ходу, и
ответ почти всегда «порядок не решает».

Замер на #62 и #83, 2500 прогонов, σ=24: сначала центр — p7 56.9%, сначала
вратарь — 56.4%. Разница 0.6 п.п., это шум.

Почему так выходит. Кросби сегодня 54.0 чистого вклада, лучший вратарь
Веймелка 45.4 — разрыв 8.6 п.п. Но Кросби возвращается к #83 в 26% прогонов,
а вратарский эшелон за Веймелкой пологий и никуда не девается: Найт (ранг 164,
вклад 41.9) доживает до #83 в 100% прогонов. Потеря от «вратарь потом» — 3.5
п.п., потеря от «центр потом» — 2.5 п.п. Они гасят друг друга.

Правило: если у второй позиции есть гарантированное дно близко к вершине,
бери первым того, у кого дна нет. Проверять — `node tools/pick.js order G C`.

## `survive` молча врал на Set

Движок держит взятых объектом `{имя:'ME'|'X'}`. Черновой скрипт дал ему `Set` —
`Object.keys(Set).length` равен нулю, драфт пошёл с первого пика, и доживаемость
вышла вдвое ниже правды: Веймелка 70% вместо 97%, Кросби 34% вместо 100%.
Ошибка не падала и выглядела правдоподобно; поймал её только сверкой с продом.
Теперь `survive` принимает оба вида. Сверка с продом — не формальность.

## дефицит крайних реален, но Кросби его не создаёт: `pick.js depth`

Довод «возьму центра — потом хороших LW/RW не останется» проверяется кривой
лучшего свободного по группам (1200 прогонов, σ=24):

    поз      #62     #83     #86    #107    #110    обрыв
    C       54.0    51.9    51.8    51.5    51.5   -2.5
    LW      51.4    48.8    48.2    46.0    45.7   -5.7
    RW      51.4    49.8    49.3    47.1    46.6   -4.8
    D       49.0    49.0    49.0    48.9    48.9   -0.1
    G       45.4    43.7    43.5    41.9    41.9   -3.5

Крайние пустеют вдвое быстрее центров — довод верный. Но из него не следует
брать крайнего сейчас, по двум причинам.

Первая: слоты. У меня C×2 уже закрыт (Маккиннон + Зибанежад C/RW). Кросби
садится в центр, Зибанежад съезжает на правый край — и съедает ровно тот слот,
который занял бы крайний. После Кросби открыто LW×2 RW×1, после Кемпе —
LW×2 RW×1, после Батерсона — LW×1 RW×2. Три крайних добирать в любом случае.
Кросби не создаёт перебора центров, потому что центром он и не закрывается.

Вторая: лучший свободный крайний сегодня — Миллер (C/LW/RW, вклад 51.5), и он
доживает до #83 в 58%. Кросби возвращается в 26%. Замер порядка: сначала
центр 58.3%, сначала крайний 57.0% — разница 1.3 п.п. в пользу центра.

Кривая рынка и очная ставка здесь расходятся: по обрыву первым надо брать
крайнего, по очной ставке — центра. Права очная ставка, потому что кривая
считает средний уровень группы, а решает судьба конкретных имён.

Из кривой следует другое, и это план на дальше: крайних брать на #83 и #86,
защитника и вратаря можно тянуть (D не проседает вовсе, у G дно Найт 41.9).

## вратаря нельзя ставить третьим: план добора подсовывает Фаулера

После Кросби (62 пика) открыто LW×2 RW×1 D×2 G×1. План «крайний, крайний,
вратарь, защитник» проверен перебором всех 12 порядков на #83/#86/#107/#110,
1500 прогонов:

    G → W → W → D   73.9      W → W → G → D   73.0   ← план Мартина
    W → G → W → D   73.8      W → D → W → G   72.4
    G → W → D → W   73.8      D → W → W → G   72.2
    W → G → D → W   73.7      D → G → W → W   72.1

Защитника последним — верно, это худшее, что можно взять рано (D не проседает:
Добсон доживает до #107 в 81%). Но вратарь обязан попасть в первые два хода,
иначе теряется 0.9 п.п.

Причина в том, что `profile` добирает по рангу и ставит вторым вратарём Джейкоба
Фаулера — проспекта без игрового времени. С ним минимум вратарских выходов
держится в 65% недель, все четыре вратарских категории падают до 36–42%.
Поэтому чистый вклад любого настоящего вратаря сейчас раздут (Веймелка 66.8
против 54.8 у лучшего крайнего): он чинит не категорию, а дыру в плане.
Пара «Миллер + Стамкос» без вратаря даёт 56.5% против 72.8% у «Веймелка +
Миллер» — это и есть размер дыры, если её не закрыть вовсе.

Правило на #83: Веймелка или Хеллебак на доске — брать их (доживают 47% и 16%).
Нет — брать Миллера, потому что Найт (63.4) доживает до #107 в 92%.

`pick.js order` теперь принимает 2–4 позиции и перебирает все порядки.

## уход Веймелки перевернул порядок; `SKIP` для тех, кого проекции не знают

На 66 пиках Веймелку забрали (6-5). Пересчёт всех 12 порядков сразу изменил
ответ на обратный: раньше вратаря нельзя было ставить третьим, теперь
«крайний, крайний, вратарь, защитник» — лучший порядок. Причина одна: ушёл
единственный вратарь, за которым стоило спешить. Оставшийся Найт (ранг 164)
доживает до #107 в 93%, спешить некуда.

Урок: порядок ходов не свойство состава, а свойство доски. Пересчитывать
`order` после каждого своего хода, а не держать план в голове.

Отдельно: Хеллебак. Ранг 53, висел свободным 13 пиков — потому что отстранён
«Джетс» за неявку в лагерь на фоне требования обмена. Проекции Yahoo этого не
знают, чужие менеджеры знают. Добавлен `SKIP='Имя;Имя'` — вычёркивает игрока из
кандидатов и из плана добора, не сдвигая номера ходов.

Грабли: вычёркивание меняет и план добора (futureFix идёт по рангу, и без
Хеллебака вторым вратарём встаёт Владарж вместо Гривза, gMinOK 0.821 против
0.793). Уровень p7 поэтому прыгает на 2–4 п.п. Сравнивать можно только порядки
внутри одного запуска, а не числа между запусками с разным SKIP.

## очко в сильной категории стоит ровно столько же, сколько в слабой

Мартин: «блоки возможно нужны, но хиты точно не нужны — у нас там и так
перебор». Звучит очевидно и неверно. Замер: берём вектор вероятностей плана
и добавляем +5 п.п. в каждую категорию по очереди.

    куда +5 п.п.   было   выигрыш p7        куда   было   выигрыш
    ppp             49%       +1.15          blk    64%     +1.18
    sho             51%       +1.15          fw     69%     +1.18
    g               54%       +1.16          hit    79%     +1.18

Разброс 0.03 п.п. Хиты на 79% стоят столько же, сколько PPP на 49% — даже
чуть больше. Причина в производной: dP(≥7 из 12)/dp_i = P(ровно 6 из прочих
11), и в неё сам p_i не входит вовсе. Категория, которую ты уже берёшь, не
обесценивается.

Верно в его доводе другое: в доминируемой категории ТРУДНЕЕ купить очки —
нормальная кривая там пологая. Вигар это преодолевает только потому, что по
хитам он выброс.

Оговорка: равенство держится на независимости категорий, которую движок
предполагает. В жизни хиты и блоки ходят вместе, и настоящий вес такой прибавки
чуть ниже расчётного.

## защитники на #110 неразличимы, и «Вигар» был моей ошибкой

Чистый вклад семи свободных защитников: Вигар 67.0, Добсон 66.9, Сандерсон
66.9, Маккавой 66.8, Моррисси 66.4, Сергачёв 66.3, Карлсон 66.3. Размах
0.7 п.п. — внутри собственной погрешности модели. Называть Вигара целью было
нельзя: `depth` печатает верх списка, а верх списка тут не значит «лучше».

По категориям Добсон бьёт или повторяет Вигара в семи из восьми (g +2.8 против
+1.3, a +5.3 против +3.8, pm +0.3 против −3.0), уступая только в хитах (+4.1
против +10.3). Одинаковый итог, но у Вигара он держится на одной категории.

## `pick.js queue`: очередь имён, а не позиций

`order` перебирает позиции, но в Fantrax очередь стоит из имён, и решает её не
первый номер, а запасной вариант — кого возьмёшь, когда первого разобрали.
Режим `queue 'А;Б;В' 'Б;А;В'` прогоняет списки целиком.

Грабли: короткая очередь кончается внутри прогона (три имени на три хода —
в 80% прогонов), и счёт считается по смещённой выборке из тех прогонов, где
все трое дожили. Списки надо давать с настоящим хвостом, иначе цифры врут.
Режим печатает долю прогонов, где очередь кончилась.

Замер очередей Мартина (2000 прогонов, #83/#86/#107, Хеллебак вычеркнут):
Миллер первым 72.0%, Батерсон первым 71.8%. Разница 0.2 п.п. — шум.

## «вбрасывания не нужны» проверено на реальном масштабе

Прошлый замер шёл на +5 п.п. и мог скрыть кривизну. Проверка на +19.5 п.п. —
ровно столько Миллер даёт в FW:

    +19.5 п.п. в одну категорию      было → станет   выигрыш p7
    fw                                 69% → 88%        +4.60
    hit                                79% → 99%        +4.61
    ppp                                49% → 69%        +4.47
    те же 19.5 размазать по четырём слабым               +4.43

Концентрация в уже сильной категории не хуже, а на волос лучше размазывания.
Довод «там и так перебор, надо в слабые» не работает ни на каком масштабе.

Что при этом верно: Батерсон как игрок лучше Миллера во всём, кроме
вбрасываний (сумма прибавок без FW: 24.9 против 22.1). Весь перевес Миллера
(68.1 против 64.2) — одна категория. По слотам они неразличимы: у обоих
остаётся LW×1 RW×1 D×2 G×1, центр Миллер не занимает, там Маккиннон и Кросби.

## сумма по категориям ничего не предсказывает: `pick.js cats`

Мартин: «почему у тебя в сравнении есть сумма? Это же так глупо. Понятно, у
кого будет фейсов и у того сумма будет самая большая». Проверил, добавляет ли
сумма прибавок по категориям хоть что-то к solo p7.

Внутри одного типа игроков — нет, вообще ничего:

```
ТОЛЬКО ПОЛЕВЫЕ: 60 игроков, корреляция 0.9999, расхождений 9/1770 (0.5%),
   из них заметных (>0.3 п.п.) 0
   худшая пара: Mark Stone p7 63.3/сумма 21.5 против Dylan Strome p7 63.3/21.4
ТОЛЬКО ВРАТАРИ: 60 игроков, корреляция 0.9999, расхождений 0/1770 (0.0%)
```

Это тот же p7 в другом масштабе. Стоя рядом с p7 в одной таблице он выглядит
как отдельное доказательство, хотя не несёт ни одного бита сверх него.

А на смеси типов он ещё и врёт:

```
80 игроков вперемешку: корреляция -0.3322, расхождений 480/3160 (15.2%),
   максимальный сдвиг в ранге 73 (Jakub Dobes)
```

Знак переворачивается, потому что вратарь двигает не одну категорию, а все
четыре сразу и через `gMinOK` — мультипликативно, а не слагаемым. Сложи
Веймелку и Миллера в одну таблицу с суммой — она расставит их задом наперёд.

Правило дома: метрика обязана предсказывать что-то, чего не говорит p7. Эта не
предсказывает. Колонки больше нет; сама таблица прибавок осталась как
`pick.js cats Имя ...` — она отвечает на «где именно он добавляет», и это
законный вопрос.

Возражение при этом было верным по выводу и неверным по причине. Сумма не
«счётчик вбрасываний»: среди полевых она совпадает с p7 ранг в ранг, то есть
не даёт Миллеру ни одного лишнего пункта сверх того, что p7 уже насчитал.
Миллер впереди Баттерсона по p7 (68.1 против 64.2) и остаётся впереди без
всякой суммы.

## вычеркнутые живут в движке, а не в двух местах

Хеллебайк отстранён «Джетс», но проекции Yahoo считают его живым, и он всю
дорогу висел наверху доски. На сайте его убрать было нечем: вычёркивание
существовало только как `SKIP='Имя'` в `tools/pick.js`. Получалось два разных
пула — сайт планировал состав с вратарём, которого взять нельзя, а консоль без
него, и числа расходились на 4 п.п.

Список переехал в `core.js` как `OUT` + `dropOut(pool, extra)`. Сайт зовёт
`NHL.dropOut(NHL.prepare(d))` и печатает, кто вычеркнут, в строке состояния;
`pick.js` зовёт `C.dropOut(POOL0, SKIP)`, где `SKIP` теперь только добавляет к
общему списку. Одно место — один пул.

## после 70 пиков защитники обошли крайних

Ушли LaCombe и Моррисси, а до них Эрик Карлссон и Теодор — четыре защитника за
полтора раунда. Чистый вклад на #83 перевернулся:

```
группа W:  Holloway 67.2 · Miller 67.2 · Martone 67.1 · Batherson 67.0
группа D:  Weegar 70.0 · Dobson 69.7 · McAvoy 69.5 · Sergachev 68.9
группа G:  Knight 67.0 · Bussi 65.9 · Hart 64.5
```

Защитник стоит на 2.7 п.п. дороже крайнего — при том, что два хода назад
порядок W→W→G→D выигрывал у всего остального. Очередь Мартина из Fantrax
(Баттерсон → Миллер → Добсон) на 600 прогонах даёт 73.2% против 70.1% у
любого варианта с третьим крайним вместо защитника. Разница не в порядке
крайних — Миллер-первым и Баттерсон-первым дают ровно одинаковые 70.1.

Третий раз подряд правильный порядок меняется от двух-трёх чужих пиков.
Пересчитывать перед каждым своим ходом, не помнить прошлый ответ.

## «мне не нравится его -22»: `pick.js off`

Возражение про плохой показатель — законное, но проверяемое. Категорию можно
обнулить ВСЕМ игрокам: тогда она у каждого 50/50 и в выборе не участвует. Если
порядок кандидатов от этого не изменился, спор идёт не о ней.

Миллер на #86, +/- −22 при моём составе, где эта категория и так провисает (45%):

```
кандидат               как есть    без pm
J.T. Miller                65.9      67.6
Porter Martone             64.2      64.7
Tom Wilson                 63.7      64.6
Nick Schmaltz              63.7      64.5
Alex Tuch                  63.7      64.4
Dylan Holloway             63.3      64.2
```

Первый тот же. Причём зазор Миллера над вторым РАСТЁТ с 1.7 до 2.9 п.п.: −22
работают против него, и он всё равно впереди. Разбор по категориям показывает
обмен целиком — Миллер отдаёт 5.7 п.п. в плюс-минусе против Мартоне и забирает
14.8 во вбрасываниях.

Перестановка Шмальца, Така и Уилсона в вердикт не идёт: они стоят на одной
десятой (63.7), это округление, а не влияние категории. Порог тот же 0.3 п.п.,
что и в проверке суммы.

Заодно: своя копия расчёта в блокноте выдала 1.2% вместо 65.9 — считать надо
тем же `pick.js`, подменяя ему данные, а не переписывать движок рядом. Проверил
оба пути: режим `off` даёт ровно те же числа, что физическая подмена файла.

## выкат рапортовал успех, когда прод отдавал старое

22.09 GitHub Pages дважды подряд уронил сборку — без единой правки, которая
могла бы этому помешать (менялись две строки в `SEED`). Ручной запрос
пересборки решил всё, ошибка была не наша. Но нашёл я её только потому, что
полез curl'ом на прод: `deploy.sh` перед этим написал «собрано».

Врал он двумя способами. Смотрел на `builds/latest` и ждал `status == built`,
не сверяя `commit` — а последней вполне могла быть прошлая, успешная сборка,
пока новая ещё не началась. И не отличал `errored` от «идёт»: упавшую сборку
он просто пережидал до таймаута и писал «сборка не дождалась».

Теперь скрипт ждёт сборку именно своего SHA, при `errored` один раз просит
пересобрать, а в конце проверяет не статус, а метку `core.js?v=` в файле,
который отдаёт прод. Статус — это чужое утверждение, файл — факт.

Тот же урок, что с замером аварии из второй точки: «сервис ответил ок» и
«сервис делает своё дело» — разные вещи.

## order и depth не видели половину кандидатов

Проверяя ход #107, наткнулся на расхождение: `solo` ставил Така первым среди
свободных крайних (66.2 против 66.1 у Холлоуэя), а `order` его вообще не
упоминал. Причина — в обоих режимах кандидаты в группе резались так:

```js
free.filter(filt(g)).sort(по rank_pre).slice(0, 14)
```

То есть сначала бралось 14 лучших по ПРЕДСЕЗОННОМУ рангу Yahoo, и только среди
них считался чистый вклад. Так по рангу 116-й — за борт. Вместе с ним за борт
уходил Вулф, который сейчас второй вратарь на доске.

Ранг отвечает на вопрос, когда игрока снимут с доски; кого брать — отвечает
вклад. Смешивать их в одном отборе нельзя. Обрезку снял, вклад считается всем
свободным в группе: 1.4 с на 600 прогонов, дорого не вышло.

Ответ изменился по существу. Было: размах порядка 2.0 п.п., крайний первым,
Овечкин на #107. Стало:

```
G → D → RW   71.9   #107 Knight 100%   #110 Weegar 98%   #131 Tuch 66%
D → G → RW   71.9   #107 Weegar 99%    #110 Knight 100%  #131 Tuch 66%
RW → D → G   71.8   #107 Tuch 83%      #110 Weegar 98%   #131 Wolf 51%
размах 0.7 п.п. — шум
```

Обрезка не просто прятала игроков, она завышала эффект порядка: сравнивались
искусственно обеднённые группы. Настоящий ответ — порядок здесь не решает.
